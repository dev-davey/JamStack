<li>
      <article class="snippet">
        <img class="snippet__image"src="{{post.data.image}}" alt="">
        <h3 class="snippet__title">{{ post.data.title }}</h3>
        <p class="spippet__meta">
          by <span>{{ post.data.author }} </span> on <span>{{post.data.date | postDate }}</span>
        </p>
        <p class="snippet__body">
          {{post.data.description}}
        </p>
        </li>
        <a href="{{ post.url }}" class="btn btn--primary">Read More</a>  
      </article>
</li>